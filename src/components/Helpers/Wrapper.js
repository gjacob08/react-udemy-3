const Wrapper = (props) => {
  // the children prop holds all the component you're
  //passing between the opening and closing tag of your custom component
  return props.children;
};
export default Wrapper;
